// Amazon Bedrock provider tests removed.
// Provider.list() now only returns Claude Agent SDK models (Anthropic provider).
// The old ai-sdk multi-provider flow that supported Amazon Bedrock is gone.
